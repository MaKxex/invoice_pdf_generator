import { useDocumentContext } from '../path/to/DocumentContext';

const ExampleComponent = () => {
  const { documentData, setDocumentData } = useDocumentContext();


  return (
    <div>
      PIZDA
    </div>
  );
};

export default ExampleComponent;